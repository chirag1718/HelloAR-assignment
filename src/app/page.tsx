import React from "react";
import SongList from "./components/SongList";

const page = () => {
  return (
    <div className="h-screen w-full">
      <SongList />
    </div>
  );
};

export default page;
